# Как собрать HomeLib Ru в `exe`

Инструкция рассчитана на человека, который не пишет на Delphi. Ниже описан
реально проверенный путь для **RAD Studio / Delphi 13 Trial**, конфигурации
**Release** и платформ **Windows 32-bit и Windows 64-bit**.

## Проверка 2.7.0_pre5.02, сборки 1069

21 сентября 2026 года через Delphi 13.2 Trial заново собраны Win32 и Win64:
основная программа 100358 строк, 0 ошибок, 0 предупреждений, 106 hints;
MCP 70047 строк, 0 ошибок, 0 предупреждений, 15 hints. Основной DPR явно
регистрирует новый `UtilsImpl\unit_PublisherSeriesSource.pas`.
`check-executable.ps1` подтвердил версию `2.7.0.1069`, архитектуру, бренд,
авторство, Common Controls 6 и PerMonitorV2 в обоих EXE.

Проверки на обеих архитектурах: SAX 291/291; MCP fixture 22, protocol 48,
extract 10, cache 43; publisher source 12. Fixture включает миграцию кэша,
откат транзакций, пересоздание БД, отмену на 108/120, продолжение оставшихся
12 книг, пропуск 120 проверенных, принудительный проход, изменение размера
и времени файла, повтор ошибок и сохранность прежних связей.
Source-тест сравнивает все 80 MiB данных, проверяет ограничение пакета 64 MiB,
очистку временных файлов и отмену списка/одиночного/пакетного извлечения 7z.

Замер ZIP 1000 книг: Win64 5885 → 719 мс, повтор 209 мс;
Win32 6131 → 624 мс, повтор 238 мс. Настоящий 7z 40 книг, Win64:
6571 → 1546 мс, повтор 10 мс. Результаты совпали с прежним способом чтения.
Кэш ОС не очищался; обе реализации используют текущий DAO; подготовка
исключена из времени. Полная коллекция 845008 книг не измерялась.
Настоящие FB2/EPUB FLibrary восстановлены обеими сборками: FB2 содержит
13 изображений с корректными ссылками, EPUB содержит 48 записей.

GUI из распакованных ZIP проверен на изолированном профиле `mcpfixture`:
заполнение шести отдельных FB2, повтор с шестью попаданиями в кэш,
Win64 принудительное перечитывание; Win32 появление издательской серии,
номера 17 и ссылок в панели книги. Флажок и кнопка помещаются в окно 786x593.
Личные коллекции не использовались. Отмена с сохранением неполного пакета
проверена автоматическим тестом, а не ручным прерыванием большой коллекции.

Итоговые архивы: `Installer\Out\pre5.02-publish` (не `pre5.02-final`, где
лежит отбракованный кандидат Win32). Проверены все 272/280 записей и их
SHA-256, состав справки из 224 файлов, неизменность комплектных читалок.
Упаковщик теперь перечисляет разрешённые файлы читалок: рабочие настройки,
история и закладки AlReader/SumatraPDF не копируются рекурсивно.

- `HomeLibRu.zip`: 23988933 байта,
  `CD51E176BC42559EF45623BAAEA77D5403D20676137B29405E0969BAAD879D38`.
- `HomeLibRu_x64.zip`: 26864586 байт,
  `A168789B633C185DFA481F5C3BBC3E5E80C1C7A0EEA8B1BA34AC12D0A5211BFC`.

ISCC не установлен: EXE-установщики не собраны и не проверены. Результаты
ниже относятся к предыдущему выпуску 1068 и сохраняются как история.

## Выпуск 2.7.0_pre5.01 от 21 сентября 2026 года

Установлена и активирована Delphi 13.2 Trial `37.0.60952.8797`. Опубликован
[HomeLib Ru 2.7.0_pre5.01](https://github.com/Dicur3x/MyHomeLib/releases/tag/2.7.0_pre5.01),
EXE `HomeLibRu.exe` версии `2.7.0.1068`. Тег указывает на коммит слияния в `master`
[`400df2f`](https://github.com/Dicur3x/MyHomeLib/commit/400df2f10aa26e8494f132c8fdb78412485a2bd5).
Разработка велась в ветке `codex/pre5-feedback-codex`, рабочее дерево:
`C:\Users\cloud\.codex\worktrees\myhomelib-pre5-feedback\MyHomeLib`.
База изменений оригинального проекта: `1bbae78` (`2.7.0_pre5`). Основное
приложение Win64/Win32 собрано в Release: 0 ошибок, 0 предупреждений,
105 hints на каждую архитектуру. Проверка GUI обеих версий пройдена.
Повторный прогон MCP после правки схемы пройден на обеих архитектурах;
финальные ZIP подготовлены, проверены и опубликованы. Ограничение отдельной
проверки первого запуска без профиля приведено в разделе «Проверенная конфигурация». Новые и
исторические результаты разделены ниже.

Владелец утвердил название **HomeLib Ru**. Архивы новой поставки:
`HomeLibRu.zip` (Win32) и `HomeLibRu_x64.zip` (Win64). Файл проекта
`Program\MyhomeLib.dproj` сохранён для совместимости, его главный исходный
файл теперь называется `HomeLibRu.dpr`.
`LICENSE` и `NOTICE` сохраняются без изменений и включаются в оба архива.

Результаты этого раздела относятся к опубликованной версии `2.7.0.1068`.
Проверки следующей версии перечислены отдельно, чтобы не смешивать сборки.

### Путь для сборки ресурсов

В установленном `brcc32.exe` 5.40 обнаружена ошибка обработки пути с каталогом
`.codex`: компилятор возвращает успешный код, но создаёт пустой `.res` размером
32 байта. Поэтому открывайте проекты в IDE через подготовленный junction
`C:\Users\cloud\HomeLibRu-build`, который указывает на рабочее дерево выше,
а не через исходный путь `.codex\worktrees`. Это те же файлы, не отдельная копия.
Перед продолжением проверьте цель junction; не удаляйте и не заменяйте
существующий каталог с этим именем без проверки.

Изображения значков теперь собираются отдельно: `MHLIconsImages.rc` →
`MHLIconsImages.res`; `MHLIcons.res` остаётся ресурсом версии DLL.
`generate_rc.js` создаёт новый RC-файл. Проверка через короткий путь дала
`MHLIconsImages.res` размером 106500 байт: все 118 ресурсов побайтно совпали
с PNG-исходниками. После каждой сборки DLL нужно проверить уже встроенные
ресурсы, а не только сообщение IDE об успехе. Для отдельного запуска BRCC
в PowerShell аргумент выходного файла следует заключать в кавычки:
`'-foMHLIconsImages.res'`.

Обе DLL значков заново собраны в Release через короткий путь и проверены:
118 изображений и два прежних служебных ресурса совпадают с прошлой
поставкой по SHA-256; добавлен только служебный `PLATFORMTARGETS` Delphi.
Разрядность, `ProductName=HomeLib Ru` и авторство проверены, DLL размещены в
обоих `Program\Out\Bin[64]\Icons`. Это подтверждение только DLL значков,
не окончания проверки основной программы или нового релиза.

### Явный ресурс манифеста

В этой IDE настройка стандартного манифеста сама по себе не обеспечила
наличие `RT_MANIFEST` в EXE. Поэтому в проекте подключён отдельный
`HomeLibRuManifest.rc` → `HomeLibRuManifest.res`, а `HomeLibRu.dpr` явно
ссылается на этот ресурс. Исходный `HomeLibRu.manifest` задаёт Common Controls
6, PerMonitorV2 и запуск с правами текущего пользователя. Значение
`Manifest_File=None` в `.dproj` теперь намеренное: оно исключает дублирование
манифеста IDE, а не отключает наш ресурс. Ресурс версии остаётся
`MyhomeLib.res`, поскольку это имя файла `.dproj`.

`scripts/check-executable.ps1` уже подтвердил встроенный манифест, версию
`2.7.0.1068`, имя HomeLib Ru и авторство в конечных EXE обеих архитектур.
Не заменяйте эту проверку просмотром настроек проекта.

## Самое важное

- Из исходников проект собирается только компилятором Delphi с VCL. Free Pascal
  и обычный Visual Studio для этого проекта не подходят.
- В Trial-версии командная сборка отключена, поэтому проекты нужно собирать в
  окне Delphi. Это ограничение лицензии Trial, а не ошибка проекта.
- Не открывайте `Program\MHL.groupproj` в Trial: в проверенной Delphi 13 Trial
  проверка зависимостей группы завершалась сбоем IDE. Три нужных проекта
  надёжнее собрать по одному, как описано ниже.
- Если RAD Studio нет, самый простой вариант — получить у разработчика уже
  собранную папку `Bin64` целиком. Один `HomeLibRu.exe` без соседних файлов —
  неполная поставка.

## Что получится

Готовая 64-битная папка находится в `Program\Out\Bin64`. Для полноценной
поставки в ней должны быть:

- `HomeLibRu.exe` — основная программа;
- `sqlite3.dll` — движок базы данных той же разрядности;
- `libzstd.dll` — распаковка каталогов нового формата metabib;
- `tools\7zip\7za.exe` — чтение книг из компактных раздач FLibrary;
- `tools\jpeg-xl\djxl.exe` — преобразование вынесенных JPEG XL-иллюстраций в
  обычный JPEG/PNG;
- `Readers\AlReader\AlReader2.exe` — чтение FB2/FBD, DOC/DOCX, RTF, TXT и
  HTML;
- `Readers\SumatraPDF\SumatraPDF.exe` — свободная переносимая читалка для PDF,
  DjVu, EPUB, MOBI/Kindle, CHM, XPS, комиксов и графических форматов; рядом с
  ней находятся тексты лицензий и список авторов;
- `MHLMcpServer.exe` — вспомогательный MCP-сервер;
- `Icons\MHLIcons.dll` — значки интерфейса;
- каталог `Help` — встроенная справка;
- файлы `genres_*.glst` — штатные списки жанров.
- `HomeLibRu.url` — ссылка на репозиторий этой ветки, добавляемая при упаковке;
- `LICENSE` и `NOTICE` — неизменные уведомления оригинального проекта.

Для Win32 аналогичная папка называется `Program\Out\Bin`. Сначала обычно
собирают Win64, а затем повторяют те же шаги с платформой Windows 32-bit.

## 1. Один раз установите зависимости

1. Установите **RAD Studio 13 / Delphi 13 (Studio 37.0)**. В установщике должны
   быть отмечены Delphi, VCL и платформа Windows 64-bit.
2. В Delphi откройте **Tools → GetIt Package Manager**, найдите
   **Virtual Treeview (Latest Version)** и установите пакет. В проверенной
   сборке использовалась версия 8.3. После установки перезапустите Delphi.
3. Компоненты Konopka/Raize (`BonusKSVC`, `RzPanel` и подобные) больше не нужны:
   проект переведён на стандартные компоненты VCL.
4. Скачайте с [официальной страницы SQLite](https://www.sqlite.org/download.html)
   архив `sqlite-dll-win-x64-...zip` и распакуйте `sqlite3.dll` в удобную
   временную папку. Для Win32 нужен архив `sqlite-dll-win-x86-...zip`.
5. Node.js нужен для пересоздания встроенных переводов и запуска тестовых
   сценариев. При его отсутствии сборочный сценарий сохраняет существующий
   языковой ресурс либо создаёт пустой; русские строки всё равно входят в EXE
   из исходников. Приватный ключ для штатных встроенных каталогов этой ветки
   не требуется. Политика upstream с обязательной подписью таких каталогов
   намеренно не перенесена.

## 2. Соберите три проекта в Delphi Trial

Каждый раз используйте **File → Open Project**, затем в правой панели проверьте:

- **Build Configurations: Release**;
- **Target Platforms: Windows 64-bit** (либо **Windows 32-bit** для второй
  сборки).

Для сборки текущего проекта используйте **Project → Build <имя проекта>**
(штатная клавиша — **Shift+F9**). После открытия справа должно быть написано
`MyhomeLib.dproj - Projects`, а не `MHLComponents.dproj - Projects`: название
окна редактора само по себе не переключает активный проект. После успешной
сборки Delphi показывает окно `Success`; предупреждения допустимы, но `Errors`
должно быть 0.

Соберите проекты в таком порядке:

1. `Program\Resources\Icons\MHLIcons.dproj` — создаёт
   `Program\Resources\Icons\Win64\MHLIcons.dll`.
2. `Program\MyhomeLib.dproj` — создаёт `Program\Out\Bin64\HomeLibRu.exe` и
   автоматически копирует рядом каталоги `Help`, `Icons` и нужную
   `libzstd.dll`.
3. `Utils\MHLMcpServer\MHLMcpServer.dproj` — создаёт
   `Program\Out\Bin64\MHLMcpServer.exe`.

После каждого проекта его можно закрыть и на вопрос о сохранении автоматически
изменённых файлов ответить **No**. Пользовательские настройки IDE для сборки не
нужны.

Для Win32 повторите этот порядок, выбрав **Windows 32-bit**. Результаты будут
находиться в `Program\Resources\Icons\Win32` и `Program\Out\Bin`.

Пакет `Components\MHLComponents\MHLComponents.dproj` для обычного EXE собирать
не требуется: используемые программой модули компонентов подключены к EXE
напрямую. Пакет нужен только разработчику, который хочет устанавливать эти
компоненты в палитру дизайнера форм.

## 3. Добавьте SQLite и проверьте всю папку

Сначала подготовьте два небольших свободно распространяемых инструмента для
поддержки раздач FLibrary:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\prepare-flibrary-runtime.ps1 `
  -Platform Win64
```

Сценарий загружает официальные 7-Zip 26.02 и JPEG XL 0.12.0, проверяет их
закреплённые SHA-256, кладёт только нужные исполняемые файлы и лицензии в
`Program\Out\Bin64\tools`. Если там уже находится другая версия, сценарий не
перезапишет её без явного `-Force`. Для Win32 замените платформу на `Win32`.
Сценарий выбирает официальный статический `djxl.exe` нужной архитектуры: x64
для Win64 и x86 для Win32. Поэтому восстановление JPEG XL работает и в
32-разрядной поставке на 32-битной Windows.

Теперь подготовьте комплектные программы для чтения. AlReader берётся из уже
имеющейся переносимой папки, а официальный SumatraPDF 3.6.1, тексты лицензий
и список авторов
скачиваются сценарием с проверкой закреплённых SHA-256:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\prepare-readers.ps1 `
  -Platform Win64 `
  -AlReaderDirectory "C:\Portable\AlReader"
```

Для второй сборки замените платформу на `Win32`. Сценарий создаёт единообразную
структуру `Readers\AlReader` и `Readers\SumatraPDF`; если AlReader уже лежит в
старой папке `Bin64\AlReader`/`Bin\AlReader`, параметр
`-AlReaderDirectory` можно не указывать.

После этого проверьте остальные файлы поставки.

Откройте PowerShell в корне репозитория. Сначала запустите безопасную проверку,
подставив свой путь к распакованной DLL:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\prepare-runtime.ps1 `
  -Platform Win64 `
  -SqliteDll "C:\Downloads\sqlite3.dll"
```

Если проверка прошла, повторите команду с `-Copy`:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\prepare-runtime.ps1 `
  -Platform Win64 `
  -SqliteDll "C:\Downloads\sqlite3.dll" `
  -Copy
```

При повторной сборке в папке `Bin`/`Bin64` могут остаться старые копии SQLite
или списков жанров. Чтобы явно заменить только проверяемые сценарием файлы их
актуальными версиями, добавьте `-Force`:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\prepare-runtime.ps1 `
  -Platform Win64 `
  -SqliteDll "C:\Downloads\sqlite3.dll" `
  -Copy `
  -Force
```

Сценарий проверяет разрядность EXE, SQLite, zstd, MCP-сервера и DLL значков,
наличие обеих читалок, справку и лицензии, копирует списки жанров и выводит
SHA-256 основных файлов. Он не перезаписывает уже существующий отличающийся
файл без явных `-Copy -Force`.

Для текущего выпуска он ожидает `HomeLibRu.exe` версии `2.7.0.1068` и
проверяет название продукта и встроенный манифест. Ключ `-Copy` также
размещает рядом точные `LICENSE` и `NOTICE` из корня репозитория;
`-Copy -Force` заменяет их только при явном повторном запуске.

Теперь запустите:

```text
Program\Out\Bin64\HomeLibRu.exe
```

При первом запуске должно появиться русское окно «Мастер создания коллекции».
Для переноса на другой компьютер копируйте всю папку `Bin64`, а не только EXE.

Для проверки поведения большой коллекции откройте вкладку «Жанры» и выберите
верхнюю группу, например «Фантастика». Она не должна сама раскрываться или
загружать книги. Кнопка «Показать» справа от названия жанра должна по явному
щелчку загрузить книги этой группы и всех её поджанров; выбор обычного поджанра
по-прежнему загружает только его книги.

Необязательная встроенная проверка MCP-сервера запускается так:

```powershell
.\Program\Out\Bin64\MHLMcpServer.exe --cache-selftest
```

Успешный результат завершается кодом 0 и содержит `"pass":true` для всех
проверок.

Разработчик может воспроизвести восстановление одной книги FLibrary без
создания или изменения коллекции:

```powershell
.\Program\Out\Bin64\MHLMcpServer.exe --restore-flibrary `
  "D:\Collection\d.usr-000001-001000.7z" `
  "Book.epub" `
  "$env:TEMP\restored.epub"
```

При успехе команда завершается кодом 0, выводит `"restored":true`, наличие и
размер обложки, а также время этапов `cover_ms`/`extract_ms`/`restore_ms`, затем
создаёт обычный FB2/EPUB по указанному третьему пути. `cover_ms` отдельно
проверяет быстрый путь панели сведений, который не пересобирает всю книгу.

## 4. Сборка из командной строки

В установленной Trial-версии этот способ **не работает**: компилятор выводит
`This version of the product does not support command line compiling`. У Trial
нет финальных консольных компиляторов — это также описано в
[справке Embarcadero](https://docwiki.embarcadero.com/Support/en/%E2%80%9CThis_version_of_the_product_does_not_support_command_line_compiling%E2%80%9D_with_a_valid_Delphi_10.1_Berlin_license).

Полностью собрать этот проект вообще без Delphi нельзя: он использует
компилятор Delphi и библиотеку VCL. В полной лицензии Delphi можно не открывать
тяжёлую IDE, а запустить **RAD Studio Command Prompt** и собрать проекты
по отдельности:

```bat
msbuild Program\Resources\Icons\MHLIcons.dproj /t:Build /p:Config=Release /p:Platform=Win64 /nologo /v:minimal
msbuild Program\MyhomeLib.dproj /t:Build /p:Config=Release /p:Platform=Win64 /nologo /v:minimal
msbuild Utils\MHLMcpServer\MHLMcpServer.dproj /t:Build /p:Config=Release /p:Platform=Win64 /nologo /v:minimal
```

Обычный терминал может не содержать переменные и пути Delphi, поэтому для
этого варианта нужен именно RAD Studio Command Prompt.

## 5. Как получить установщик

Для собственного использования достаточно проверенной папки `Bin64`. Если
нужен установщик:

1. Установите [Inno Setup 6](https://jrsoftware.org/isinfo.php).
2. Сначала полностью подготовьте папку `Bin64`, как описано выше.
3. Из корня проекта выполните `Installer\build_installer.cmd x64`.

Варианты команды: `x86`, `x64` или `all`. Результат появляется в
`Installer\Out`.

Имена текущих установщиков: `Setup_HomeLibRu_2.7.0_pre5.01.exe` и
`Setup_HomeLibRu_2.7.0_pre5.01_x64.exe`. Готовность установщиков в этой задаче
ещё не подтверждена сборкой.

`Installer/LegacyUpgrade.iss` удаляет старый `MyHomeLib.exe` только после
установки нового EXE, если прежняя запись удаления с тем же AppID указывает
ровно на текущую папку установки. Старые ярлыки удаляются только при
подтверждённой ссылке на этот старый EXE. Профили, коллекции и общие файлы не
затрагиваются. Отдельный `Installer/tests/LegacyUpgradeCompile.iss`
предназначен только для проверки компиляции, запускать его нельзя. На этой
машине ISCC пока отсутствует; компиляция и реальное обновление установщиком
не проверены. Этот выпуск опубликован в виде двух ZIP, без установщика.

## 6. Как подготовить архивы релиза

После свежей сборки, регрессии и подготовки обеих папок runtime выполните:

```powershell
.\scripts\package-release.ps1 -Platform Win32 -OutputDirectory .\Installer\Out\pre5-final
.\scripts\package-release.ps1 -Platform Win64 -OutputDirectory .\Installer\Out\pre5-final
```

Сценарий повторно запускает проверку runtime и создаёт
`Installer\Out\pre5-final\HomeLibRu.zip` и
`Installer\Out\pre5-final\HomeLibRu_x64.zip`. Он берёт перечисленные
файлы и штатные каталоги поставки, а не всю папку запуска. Перед упаковкой
убедитесь, что в `Readers`, `Help`, `Icons` и `tools` не попали личные файлы
или настройки от проверочных запусков. Пустой штатный маркер AlReader
`$savevtut.ini` является частью portable-комплекта.

Существующий архив не перезаписывается. Для повторной упаковки задайте
отдельную папку параметром `-OutputDirectory`. `LICENSE` и `NOTICE`
включаются без изменений. Финальные архивы в `pre5-final` созданы и проверены:

| Архив | Размер, байт | SHA-256 |
| --- | ---: | --- |
| `HomeLibRu.zip` | 23932852 | `0DFC91A18DB7CDF6EB7C3E061BBBC3A521512C197ACD10D9BF83B0FE3978FDAB` |
| `HomeLibRu_x64.zip` | 26798475 | `0476A0B5EA788F0627059A24C1CE5210547DC7853112D8BB42FA29AB930DD33D` |

В ZIP проверены 272 записи Win32 и 280 записей Win64: содержимое совпало
с подготовленными runtime по SHA-256. Все 224 файла справки соответствуют
текущим исходникам. Эти архивы опубликованы в
[выпуске 2.7.0_pre5.01](https://github.com/Dicur3x/MyHomeLib/releases/tag/2.7.0_pre5.01).

Ранние архивы в стандартной папке `Installer\Out\2.7.0_pre5.01` устарели;
для публикации их использовать нельзя. Параметр `-OutputDirectory` выше
намеренно отделяет итоговую поставку от этих кандидатов.

ZIP распаковывайте в чистую папку, не поверх старой версии. Перед переходом
закройте обе программы и сохраните резервную копию `%APPDATA%\MyHomeLib`
и отдельных `.hlc2`. Обычный профиль по-прежнему имеет имя MyHomeLib и
используется новой программой без переименования. Для portable-профиля
перенесите из резервной копии собственный `myhomelib2.ini`, папку `Data`
и `presets.cxml2`; рядом с EXE должен быть пустой маркер `uselocaldata` без
расширения (либо такой параметр запуска). Один INI рядом с EXE portable-режим
не включает. Затем проверьте пути к коллекциям. Упаковка и распаковка ZIP
не удаляют старые EXE, профили или пользовательские файлы автоматически.

Возврат к старому EXE выполняется только с отдельной резервной копией профиля
и коллекций, сделанной до обновления. Старые версии не обслуживают таблицы
издательских серий: полное пересоздание каталога старой программой может
оставить связи по BookID от прежних книг. Не используйте старую и новую
версии по очереди для изменения одной рабочей `.hlc2`.

## Частые ошибки

### Не найден `VirtualTrees`, `VirtualTreesD` или `VirtualTreesR`

Virtual Treeview не установлен для Delphi 13 либо Delphi не была перезапущена
после GetIt. Установите пакет заново и не используйте готовые DCU/BPL от другой
версии Delphi.

### Delphi Trial падает при открытии или сборке группы

Не используйте `Program\MHL.groupproj`. Закройте Delphi и соберите три `.dproj`
по отдельности в порядке из раздела 2.

### Ошибка завершающего шага `copy_help`, `copy_icons` или `post_build`

Проверьте, что проект значков был собран первым и существует файл
`Program\Resources\Icons\Win64\MHLIcons.dll`. Для копирования справки также
нужен штатный `C:\Windows\System32\robocopy.exe`.

### Программа сообщает, что не найдена `sqlite3.dll`

DLL не скопирована рядом с `HomeLibRu.exe` либо имеет другую разрядность.
Повторите раздел 3 и используйте именно x64-DLL для Win64.

### Импорт metabib сообщает, что не найдена `libzstd.dll`

Сначала снова соберите `Program\MyhomeLib.dproj`: завершающий шаг копирует
`Program\Resources\zstd\Win32` или `Win64\libzstd.dll` рядом с EXE. Не
копируйте DLL другой разрядности — `prepare-runtime.ps1` это обнаружит.

### Не открывается книга из архива 7z

Запустите `scripts\prepare-flibrary-runtime.ps1` для нужной платформы и
проверьте наличие `tools\7zip\7za.exe` рядом с программой. Установленный в
системе 7-Zip также распознаётся, но для переносимой поставки рекомендуется
штатная папка `tools`.

### Книга открылась без вынесенных иллюстраций

Проверьте, что рядом с корневыми архивами раздачи скачаны соответствующие ZIP
в папках `covers` и `images`, а рядом с программой есть
`tools\jpeg-xl\djxl.exe`. Незавершённый боковой архив намеренно пропускается:
программа позволяет читать текст, пока торрент продолжает загружаться.

### Программа запускается без иконок

Убедитесь, что рядом с EXE есть `Icons\MHLIcons.dll`, а затем повторно соберите
`Program\MyhomeLib.dproj`, чтобы его завершающий шаг скопировал актуальную DLL.

Если при запуске появляется сообщение о ресурсе вроде
`LIGHT_FILETYPE_FILETYPE_FB2`, DLL значков устарела. Снова соберите сначала
`Program\Resources\Icons\MHLIcons.dproj`, затем `Program\MyhomeLib.dproj`.
Файлы `Program\Resources\Icons\Win64\MHLIcons.dll` и
`Program\Out\Bin64\Icons\MHLIcons.dll` после этого должны совпадать.

## Проверенная конфигурация

### Проверки нового выпуска `2.7.0_pre5.01`

На 21 сентября 2026 года основные EXE обеих архитектур скомпилированы,
проверка GUI пройдена. Заключительный повтор MCP после исправления схемы
тоже пройден. Готовые ZIP проверены по составу и SHA-256 и опубликованы.

Обе финальные распаковки ZIP дополнительно проверены с тестовым профилем
`mcpfixture`, созданным MCP из соответствующего архива, без копирования личных
настроек. Программа открывается; русские тексты и кнопки, сведения об авторе,
сериях и жанре в панели книги и версия `2.7.0.1068` отображаются. Мастер
создания коллекции, вызванный из главного окна через меню или `Ctrl+N`, виден
и работает на обеих архитектурах: x64 прошёл приветствие и выбор типа с
последующей отменой, x86 — приветствие и отмену. Коллекции не создавались и
не удалялись; все проверочные окна закрыты.

Ограничение осталось только у первоначального запуска без коллекций:
на обеих архитектурах программа дошла до автоматического открытия мастера,
а для x64 отладчик подтвердил штатное ожидание действий в диалоге без
исключения. Инструмент проверки окон не подтвердил видимость именно этого
первоначального диалога во время создания главного окна. Этот сценарий не
отмечен как полностью визуально проверенный; подтверждённой ошибки запуска
также нет. Вызов мастера из уже открытого главного окна проверен успешно.

Основной проект: Release Win64/Win32, 99240 строк, 0 ошибок,
0 предупреждений, 105 hints на платформу. `check-executable.ps1` подтвердил
PE-разрядность, версию, бренд/авторство, Common Controls 6 и PerMonitorV2.
`BookInfoPanelTest` выполнил 273 проверки на каждой архитектуре без ошибок;
`MetabibTest` прошёл 34 сценария на каждой архитектуре.

В GUI обеих версий проверены переходы автор/серия/жанр/издательская серия,
заполнение 6 из 6 книг без ошибок, номера 1/2/17, поиск и выбор столбцов,
показ 3/4 строк при переключении удалённых книг и сохранение вкладки/книги
после повторного запуска. Проверены буквальные символы в названиях и интервалы
панели при DPI-масштабировании. В x64 проверено окно «О программе»:
HomeLib Ru, `2.7.0.1068`, исходное авторство. Ошибка запуска при сохранённом
авторе с буквой вне алфавитной панели исправлена резервным выбором всех букв.

На окончательных `MHLMcpServer.exe` Win64 и Win32 подтверждены 48 протокольных
сценариев, 22 fixture-проверки, 10 extract-проверок и 43 cache-проверки прошли.
Fixture-набор проверяет отдельное хранение издательских серий и настоящий
фоновый индексатор: отмена после 108 из 120 книг сохраняет только завершённые
книги, полный повтор индексирует все 120, отсутствующие и повреждённые файлы
не стирают прежние связи, а корректный FB2 без издательской серии очищает их.
Это не заменяет проверку вкладки и панели книги в основном приложении.
Окончательный MCP собран в Release на обеих платформах: 67886 строк,
0 ошибок, 0 предупреждений, 11 hints. Все наборы повторно прошли после
добавления регрессии пересоздания `.hlc2`: повторно использованный BookID
не наследует издательские связи старой книги.
Подготовка runtime с `-Copy -Force` уже прошла на обеих архитектурах;
224 файла справки совпали с текущим `Program/Help` по относительным именам
и SHA-256. `prepare-runtime.ps1` теперь отвергает устаревшую справку, а
также неверный бренд вспомогательных MCP/Icons-бинарников.
Также на обеих архитектурах повторена проверка реальных файлов FLibrary:
FB2 получил 13 изображений, восстановленный EPUB содержит 48 записей и
4 изображения, а проверка ссылок в 37 XML/XHTML-документах не нашла
отсутствующих внутренних файлов.

- Заставка, окно «О программе» и версия файла должны показывать
  `2.7.0.1068`.
- Основной EXE называется `HomeLibRu.exe`, продукт и заголовок релиза:
  HomeLib Ru. Проверить `ProductName`, `InternalName`, `OriginalFilename`,
  `LegalTrademarks` и сохранение авторства Oleksiy Penkov в `LegalCopyright`
  и окне «О программе». Проверить также ресурсы собственных DLL и MCP-сервера.
- В самом EXE должен присутствовать ресурс манифеста с зависимостью
  `Microsoft.Windows.Common-Controls` версии `6.0.0.0`. В исследованном
  прежнем Win64 EXE `2.7.0.1067` эта зависимость отсутствовала; это связано с
  исчезнувшими ссылочными элементами `TLinkLabel` в панели книги. Успешной
  компиляции недостаточно: проверить видимость текста и переходы по ссылкам
  авторов, серий и жанров в запущенной программе.
- Проверить отдельную вкладку издательских книжных серий, строку в панели
  книги и переключатель удалённых книг,
  включая авторов, у которых остались только удалённые книги.
- Повторить проверки комплектности, INPX/ZIP, FLibrary, мультисерийного
  поиска, жанров, читалок и MCP из `ROADMAP.md`.

Для проверки metabib откройте в IDE отдельный проект
`tools\metabib\tests\MetabibTest.dproj`, выберите Release/Win64 и соберите
его. Он использует только RTL/Winapi и модули репозитория, не требует
VirtualTreeView или сборки пакета компонентов. Затем из корня репозитория:

```powershell
node tools\metabib\tests\reader_tests.js tools\metabib\tests\Out\Win64\Release\MetabibTest.exe
```

Повторите для Win32 с соответствующим путём. Все 34 сценария уже прошли на
свежих harness обеих архитектур. Набор проверяет:
пустые и повреждённые значения, приоритеты источников аннотаций и метаданных,
идентификаторы книги, авторов, сохранение всех серий выбранного источника
и отдельные издательские серии.
Сценарии создают отдельные тестовые данные и не меняют пользовательские
коллекции. Проверки обычного JSONL не требуют `libzstd.dll` рядом с harness.

### Результаты предыдущих выпусков

5 сентября 2026 года владелец заново собрал из финальных исходников и вручную
проверил Release/Win64 и Release/Win32 `MyHomeLib.exe` версии `2.7.0.1067`:

- обе программы запускаются и работают штатно;
- верхний жанр не раскрывается и не запускает тяжёлую выборку автоматически;
- кнопка «Показать» присутствует и явно загружает книги верхнего жанра;
- обновлённый раздел «О программе» отображается;
- `prepare-runtime.ps1` повторно подтвердил PE-разрядность и полный комплект
  закреплённых зависимостей для обеих платформ; после публикации Win32-комплект
  дополнительно переведён на официальный статический x86-декодер JPEG XL
  0.12.0, поэтому картинки FLibrary восстанавливаются и на 32-битной Windows.

4 сентября 2026 года прежняя рабочая ветка
`codex/flibrary-multiseries-compat-codex` проверена в RAD Studio 13 Trial для
Release/Win64 и Release/Win32:

- обе версии `MyHomeLib.exe`: `Success`, 0 ошибок, 0 предупреждений;
- `MHLMcpServer.exe`: `Success`, 0 ошибок, 0 предупреждений;
- `prepare-runtime.ps1` подтвердил разрядность и полный набор SQLite, zstd,
  значков, 7-Zip и JPEG XL для обеих платформ;
- на обеих платформах прошли 47 протокольных/поисковых сценариев, fixture-, 10
  extract-тестов и полный набор cache-тестов;
- реальный FB2 прочитан из 7z и получил 13 корректных JPEG/PNG;
- реальный вложенный 7z-EPUB пересобран в стандартный EPUB из 48 записей,
  получил 4 JPEG вместо JXL, не содержит битых внутренних ссылок и открылся в
  AlReader;
- после однопроходной распаковки тот же EPUB восстанавливается примерно за
  1,72 с вместо 36 с, а его обложка отдельно готовится примерно за 54 мс;
- обычная INPX/ZIP-коллекция и мультисерийный поиск проверены отдельно.

Владелец вручную подтвердил работу обеих сборок, включая FLibrary, изображения,
мультисерийный флажок и ускоренную навигацию по большой коллекции.

29 августа 2026 года этот порядок был повторно проверен на RAD Studio 13 Trial:

- `Release`, `Windows 32-bit` и `Windows 64-bit`;
- Virtual Treeview 8.3 из GetIt;
- отдельная сборка DLL значков, основной программы и MCP-сервера;
- обе DLL значков: `Success`, 0 ошибок и 0 предупреждений;
- оба основных проекта: `Success`, 0 ошибок и 0 предупреждений;
- оба MCP-сервера: `Success`, 0 ошибок и 0 предупреждений;
- `prepare-runtime.ps1` подтвердил правильную разрядность и полный набор
  зависимостей в `Bin` и `Bin64`, включая SQLite, zstd и DLL значков;
- изолированный запуск обеих версий открывает русское главное окно и настройки
  без ошибки `Stream read error`; в x64 дополнительно проверены окно
  «О программе» и замена поискового запроса через `Ctrl+A`;
- встроенная проверка `MHLMcpServer.exe --cache-selftest` полностью проходит
  и завершается кодом 0;
- все MCP-протокольные, поисковые, FB2, fixture- и cache-тесты проходят в x32
  и x64; тесты работают на отдельной библиотеке из шести книг и не используют
  пользовательские коллекции.

Конвертеры FB2 → MOBI/EPUB/PDF и некоторые программы чтения являются отдельными
сторонними продуктами. Для сборки и первого запуска HomeLib Ru они не нужны.
